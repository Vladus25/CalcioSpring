import { RuoliUniciPipe } from './ruoli-unici.pipe';

describe('RuoliUniciPipe', () => {
  it('create an instance', () => {
    const pipe = new RuoliUniciPipe();
    expect(pipe).toBeTruthy();
  });
});
